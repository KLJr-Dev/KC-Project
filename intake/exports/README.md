# Intake export tree (Cycle-9)

Intended packages live under `/app/exports/{request_id}/`.

**Traversal plant (F3):** out-of-tree file at `/app/private/onboarding-export.flag`.  
From an approved request export root, practice:

- `file=../../private/onboarding-export.flag`
- `file=..%2F..%2Fprivate%2Fonboarding-export.flag`

Pending requests must not serve exports (D2b).
