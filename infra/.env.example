# v1.0.0 production env template (intentional weak defaults — CWE-798)
# Copy to infra/.env before: docker compose -f infra/docker-compose.prod.yml up -d --build
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=kc_prod
PORT=4000
NEXT_PUBLIC_API_URL=/api
