# v0.7.x — Docker Deployment Summary

## Deliverables

- `backend/Dockerfile`, `frontend/Dockerfile`, `.dockerignore`
- `infra/docker-compose.prod.yml` (postgres, backend, frontend, nginx)
- `infra/nginx.conf`, `infra/.env.example`, `infra/vm-setup.sh`
- `infra/smoke-test.sh`

## Run

```bash
docker compose -f infra/docker-compose.prod.yml up -d --build
./infra/smoke-test.sh
```

Access: http://localhost:8080 (nginx → frontend, /api → backend)

## CWEs

- CWE-798: Hardcoded DB/JWT defaults in compose templates
