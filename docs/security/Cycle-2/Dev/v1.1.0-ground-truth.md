# v1.1.0 Ground Truth (Developer Cheat Sheet)

**Spoilers.** Canonical plant/break reference for Cycle 2.  
Design authority: [v1.1.0-box-plan.md](v1.1.0-box-plan.md).  
Player-facing (no spoilers): [v1.1.0-player-brief.md](v1.1.0-player-brief.md).

**Branch:** `ctf/v1.1.0` · **Base:** tag `v2.0.0`  
**Status:** Scaffold — update each section when the matching plant/break lands.

---

## 1. Quick reference (TBD as implemented)

### Demo / CTF accounts

| Role | Email | Password | User ID | Notes |
|------|-------|----------|---------|-------|
| User | `user@kc.test` | *(v2.0.0 seed)* | 9001 | May remain; hydra target TBD |
| Moderator | `mod@kc.test` | | 9002 | |
| Admin | `admin@kc.test` | | 9003 | Owns `proof.txt` |
| Other | `other@kc.test` | | 9004 | Owns `local.txt` (9104) |

### Flags

| Tier | Locus | Hex (dev placeholder) | Implemented |
|------|-------|----------------------|-------------|
| User | `local.txt` / file 9104 | `a1b2c3d4e5f60718293a4b5c6d7e8f90` | [ ] |
| Admin | `proof.txt` (admin-owned) | `0f1e2d3c4b5a69788796a5b4c3d2e1f0` | [ ] |
| DB | row TBD | `9c8b7a69584736251e0f1d2c3b4a5968` | [ ] |

### Breaks

| ID | Break | File(s) | Done |
|----|-------|---------|------|
| H1 | Horizontal → local.txt | TBD | [ ] |
| V1 | Vertical → admin | TBD | [ ] |
| I1 | CTF compose (5433 + loot) | `infra/docker-compose.ctf.yml` | [ ] |

---

## 2. Proof commands (examiner)

### User

```bash
# after low-priv login — identity
curl -sS -H "Authorization: Bearer $USER_TOKEN" http://127.0.0.1:8080/api/auth/me
# flag
curl -sS -H "Authorization: Bearer $USER_TOKEN" \
  http://127.0.0.1:8080/api/files/9104/download
```

### Admin

```bash
curl -sS -H "Authorization: Bearer $ADMIN_TOKEN" http://127.0.0.1:8080/api/auth/me
# download proof.txt by id once seeded
```

### DB

```bash
PGPASSWORD='<from admin loot>' psql -h 127.0.0.1 -p 5433 -U postgres -d kc_prod \
  -c 'SELECT current_user;' \
  -c 'SELECT /* flag query TBD */;'
```

---

## 3. Diff from v2.0.0

*(Fill as PRs land on `ctf/v1.1.0`.)*

| Area | v2.0.0 | v1.1.0 CTF |
|------|--------|------------|
| File ownership | Enforced | TBD break |
| HasRoleGuard | DB role | TBD break |
| Postgres publish | No (prod) | CTF overlay `:5433` |
| Auth throttle | Strict | Softened on CTF |
| Seeds | Demo secrets text | OSCP hex files + DB row |

---

## 4. Related

- Box plan: [v1.1.0-box-plan.md](v1.1.0-box-plan.md)
- Cycle index: [../README.md](../README.md)
- Secure baseline (what we open): [../../../spec/security-baseline.md](../../../spec/security-baseline.md)
