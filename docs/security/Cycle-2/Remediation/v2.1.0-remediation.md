# Remediation Report — KC-Project v2.1.0

**Cycle:** 2 · **Source pentest:** [../PenTest/v1.1.0-writeup.md](../PenTest/v1.1.0-writeup.md)  
**Fork / harden from:** secure tag `v2.0.0` + close CTF breaks introduced on `ctf/v1.1.0`  
**Status:** Planning — implement after writeup sign-off; then tag **`v2.1.0`**

---

## Finding → fix map

| ID | Severity | Fix intent | Likely files / surfaces | Acceptance test |
|----|----------|------------|-------------------------|-----------------|
| C2-F01 | Critical | Enforce ownership (or DB admin/mod) on file get/download/delete; never no-op `assertCanRead` outside explicit CTF lab | `backend/src/files/files.service.ts`, `ctf-mode` gates | User B → `GET /files/{A}` = **403**; CTF e2e only when `CTF_MODE` |
| C2-F02 | Critical | Authorize admin routes from **DB role**; JWT `role` non-authoritative; RS256 (or strong secret) on secure path | `has-role.guard.ts`, `jwt-config.ts`, compose secrets | Forged `role:admin` + DB `user` → **403**; secure compose no weak HS256 |
| C2-F03 | Critical | Do not publish Postgres on secure compose; remove DB password from downloadable seeds; app DB role ≠ superuser | `docker-compose.prod.yml`, CTF overlay only for `:5433`, seed migrations | Host `:5433` closed on secure stack; no `DB_PASSWORD=` in user files |
| C2-F04 | Medium | TLS by default for non-loopback demos; `Secure` cookies | `docker-compose.tls.yml`, cookie util, nginx | HTTPS login; no cleartext Bearer on wire |
| C2-F05 | Info | Keep throttles; document | throttler + nginx | Still 429 under burst; authz tests independent |
| C2-F06 | Info | Client refresh UX / operator docs | frontend auth, docs | Stale token behavior documented |
| C2-F07 | Info | Least-privilege DB + monitoring | Postgres roles, runbooks | App cannot `DROP DATABASE` |

---

## Short / medium / long

See writeup §11. Operational checklist:

1. [ ] Close C2-F01 / F02 on code paths used by secure compose  
2. [ ] CTF overlay remains opt-in only (`docker-compose.ctf.yml`)  
3. [ ] Rotate planted hexes + DB password after public playtests  
4. [ ] Delete lab user `pentest@lab.local` on reset  
5. [ ] Full e2e + journey green on secure path  
6. [ ] Tag `v2.1.0` when gate signed  

---

## Out of scope for v2.1.0

- Re-opening Cycle-1 kitchen-sink surface  
- XSS/CSRF deep dive (future cycle unless residuals appear)  
- Changing product feature set beyond security controls  
