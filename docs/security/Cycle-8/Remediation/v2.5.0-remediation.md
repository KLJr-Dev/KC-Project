# Remediation Report — Cycle-8 `v2.5.0`

**Cycle:** 8 · **Source pentest:** [`ctf/v1.5.0` writeup](../PenTest/v1.5.0-writeup.md)  
**Branch:** `remediation/v2.5.0` · **Blue plan:** [blue-team-plan.md](blue-team-plan.md)  
**Residuals:** [accepted-residuals.md](accepted-residuals.md)  
**Gate:** [v2.5.0-secure-ready.md](../../../release/v2.5.0-secure-ready.md)

---

## Finding → fix map

| ID | CWE | OWASP 2025 | Milestone | Status | v1.5.0 weakness | v2.5.0 fix (intent) | Primary files | Verification |
|----|-----|------------|-----------|--------|-----------------|---------------------|---------------|--------------|
| C8-F01 | 89 | A05 Injection | M1 | **Verified** | Intake `q` SQLi → full `mail_users` dump | Parameterized queries; remove F1 plant from tip | `intake/`, seed SQL | `cycle8-blue-assert.sh` |
| C8-F02 | 521 | A07 Auth Failures | M2 | **Verified** | Weak FTP `lisa`/`peanut` | No LIVE weak FTP on secure tip; `:21` off prod | overlay retired from tip | `assert-cycle8-unpublished.sh` |
| C8-F03 | 434 | A06 Insecure Design | M2 | **Verified** | FTP `www/` → PHP RCE | Separate upload from execution; no webshell path | overlay retired from tip | Unpublished assert |
| C8-F04 | 269 | A02 Misconfiguration | M2 | **Verified** | `ops` NOPASSWD `nano` → root | Remove privesc plant on secure tip | overlay retired from tip | Unpublished assert |
| C8-F05 | 284 | A01 Broken Access Control | M2 | **Verified** | Edge → internal Samba + cred reuse | Overlays internal-only on archive; not on prod | overlay retired from tip | Unpublished assert |
| C8-F06 | — | — | — | **Residual** | Cowrie `:22` decoy | Archive on `ctf/v1.5.0` only | overlay | Document residual |
| C8-F07 | 200 | A06 Insecure Design | M2 | **Verified** | Decoy legacy FTP creds in Intake UI | Remove decoy panel | `frontend/app/intake/` | `cycle8-blue-assert.sh` |

---

## Per-finding detail

### C8-F01 — Intake SQLi (must-close)

**v1.5.0 state:** Authenticated `GET /api/intake/search?q=` allows UNION-based extraction of `public.mail_users` including F1.

**v2.5.0 intent:** Keep Intake search as a product feature. Use parameterized queries (or ORM). Remove graded flag row / exploitable plant from secure seed. Flags remain on `ctf/v1.5.0` only.

### C8-F02 … C8-F05 — Overlay chain (must-close on tip)

**v1.5.0 state:** `docker-compose.cycle8.yml` publishes FTP `:21`, Cowrie `:22`, edge with sudo nano, internal Samba/mail with F2–F5 plants.

**v2.5.0 intent:** Default `docker-compose.prod.yml` never publishes overlay services (`assert-cycle8-unpublished.sh`). Overlay + plants stay on **`ctf/v1.5.0`** for replay. Tip UI / version → `v2.5.0`. Northwind skin retained.

### C8-F06 — Cowrie decoy

Info observation. Accepted residual once overlays unpublished on secure tip.

---

## Waves (execution checklist)

### Wave A — M1

- [x] Parameterize Intake search / remove SQLi plant
- [x] Wire hardened Intake on prod compose + nginx `/api/intake/`
- [x] Negative test: sqlmap-style dump fails on secure tip (M3 assert)
- [x] No `OS{` F1 in API responses on prod compose

### Wave B — M2

- [x] Confirm prod compose has no cycle8 published ports (`:21`, `:22`, …)
- [x] Remove / isolate F2–F5 plants from secure default path (overlay deleted from tip)
- [x] Tip UI version `v2.5.0`
- [x] Decoy Intake panel cleaned up (C8-F07)

### Wave C — M3

- [x] Wire `assert-cycle8-unpublished.sh` into gate / smoke (PASV 30000–30009 check added)
- [x] Add `cycle8-blue-assert.sh` (Intake negative + no tip flags)
- [x] Wire `cycle8-blue-assert.sh` into `smoke-test.sh`

### Wave D — M4

- [x] Sign [v2.5.0-secure-ready.md](../../../release/v2.5.0-secure-ready.md)
- [x] PR → `main` · tag `v2.5.0` · freeze branch · Cycle-8 Closed

---

## References

- Red writeup: frozen on `ctf/v1.5.0`
- Player brief / GT: archive `ctf/v1.5.0` only
- Do not re-break Cycle-6/7 Blue controls
