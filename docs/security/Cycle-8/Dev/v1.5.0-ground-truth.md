# Cycle-8 ground truth — `v1.5.0` (examiner)

**Classification:** Examiner / Dev only — do not ship in player packets.  
**Plants:** FastAPI Intake (behind Nest) + compose overlays (see [cycle-8-decisions.md](cycle-8-decisions.md) — **P0 FINAL**)

---

## Flags

| ID | Value | Location |
|----|--------|----------|
| F1 | `OS{0036b6ceb86445a4c8dce300e4205c43}` | sqlmap dump (Intake DB) |
| F2 | `OS{4af4a36815ce627e5d3eba01b57e9376}` | **Post-Hydra FTP** (authenticated loot) |
| F3 | `OS{e0af60da9c8aa1daa4a79f2cb95478d2}` | Edge `~/user.txt` (interactive revshell) |
| F4 | `OS{44d562a5ae240a23b8bbf1c21c605fc3}` | `/root/root.txt` after sudo nano |
| F5 | `OS{d310a0605d95303aa114d707b7686f76}` | Internal Samba and/or SMTP (via pivot) |

## Intended path

1. **F1:** Portal → `/api/intake` (proxied FastAPI) → **sqlmap** → F1 + L1 users + L2 mail hashes.  
2. **(no flag):** **John** → L2 SMTP cleartexts (save).  
3. **F2:** **Hydra** weak FTP (L1 users, L3 passwords ≠ L2) → F2 + webroot write (or equiv.).  
4. **F3:** Simple webshell → listener → revshell → `user.txt`. (`:22` Cowrie = decoy.)  
5. **F4:** sudo nano → `root.txt` + L4 **partial** Samba hints (no full SMTP gift here).  
6. **F5:** Pivot → Samba enum + SMTP with John’d creds. Kali must not reach Host B directly.

## Credential ledger (fill cleartexts at P2)

| ID | Artifact | Class | Consumed by | Build note |
|----|----------|-------|-------------|------------|
| L1 | ~12–20 names | LIVE list | Hydra `-L` / John scope / VRFY | Shared universe |
| L2 | SMTP/mail hashes → cleartexts | LIVE | G6 SMTP | ≠ FTP passwords |
| L3 | Weak FTP passwords | LIVE | Hydra → F2 + shell | ≠ L2 |
| L4 | Partial Samba hints in root | LIVE | G6 SMB enum | Not full creds map |
| D1 | Cowrie | DECOY | — | Timebox |
| D2 | Fake admin API creds | DECOY | — | Fail on FTP/SMTP |
| D3 | Uncrackable hashes | NOISE | — | John timebox |
| D4 | `ceo@northwind…`-style addresses | NOISE→assist | SMTP priority | From dump |
| DEMO | Nest demo users | DEMO | UI only | Never G3–G6 |

**Orphan check:** every LIVE has Consumed by.

## Controls that must still hold

| Control | Expectation |
|---------|-------------|
| Prod compose alone | No `:21` / `:22` / Host B publish; no `:5433`; no Intake plants |
| Ops / Preview / Notes | Prior Blue controls remain |
| Blue `v2.5.0` | Plants closed; overlays unpublished; **Northwind skin kept** |

## Examiner dry-run (sketch — P2)

```bash
docker compose -f infra/docker-compose.prod.yml -f infra/docker-compose.cycle8.yml up -d --build
./infra/assert-pg-unpublished.sh
./infra/smoke-test.sh
./infra/cycle8-examiner.sh
./infra/journey-test.sh
```

### Must-fail

- Graded shell via SSH / Cowrie  
- FTP foothold without Hydra (gift cleartext in anon loot)  
- John password works as FTP password (redundancy bug)  
- Kali → Samba/SMTP without pivot  
- Shell without FTP→webroot (or designed equiv.) path  

## Explicit non-goals

Full AD · Windows VM · Word/macros · graded Cowrie · docker escape · Ops LFI / Preview / CSRF / Notes re-break · parallel public FastAPI product port.
