# Cycle-7 box plan — `v1.4.0` (Northwind Ops)

**Baseline before expansion:** tag **`v2.3.0`**  
**Pair:** intentional insecure **`v1.4.0`** → hardened **`v2.4.0`** ([ADR-035](../../../decisions/ADR-035-cycle-7-multi-service-pair.md))  
**Consumes:** FC-18 (LFI), FC-14 (FTP overlay)  
**Decisions:** [cycle-7-decisions.md](cycle-7-decisions.md)

---

## Story (one graded path)

1. Recon Host-Only target — **nmap**/Nikto/gobuster; note `:8080`, `:21`, `:2222`, `:2223`.  
2. Authenticate to KC → **Ops Documents** LFI → **F1**.  
3. Anon **FTP** → loot (**F2**) + SSH creds.  
4. Timebox **Cowrie** on `:2223` (decoy); use **real SSH** `:2222` → **F3** (`user.txt`).  
5. `sudo -l` → **find** GTFO → **F4** (`root.txt`).  
6. Discover internal jump; **SSH tunnel** → basic-auth intranet → **F5**.  
7. Stop. Writeup. No AD / Windows VM.

```text
Kali → :8080 LFI → F1
     → :21 FTP → F2 + creds
     → :2223 Cowrie (dead end)
     → :2222 SSH user → F3 → sudo find → F4
     → tunnel → internal jump HTTP → F5
```

---

## Ceiling

| In | Out |
|----|-----|
| LFI, FTP, SSH, sudo GTFO, dual-home pivot | Full AD / Kerberos |
| Cowrie as decoy | Real Windows VM / WinRM stack |
| Five `OS{32hex}` flags | Docker escape (FC-13) |
| Multi-day Red bar | Preview SSRF / CSRF / Notes XSS re-break |
| Compose overlays | Published Postgres on prod alone |

---

## Difficulty

**Medium–hard OSCP flavor:** multi-service enum, finish LFI, spray discipline, PrivEsc, pivot. Expect **several sessions**, not a same-day clear.

---

## Hint ladder (player brief)

1. New document/viewer features often take a path parameter.  
2. Forgotten file services still show up on full port scans.  
3. Not every SSH listener is your foothold.  
4. User shell is not the end — check sudo.  
5. Some hosts are only one hop away.

Spoilers stay in ground truth.
