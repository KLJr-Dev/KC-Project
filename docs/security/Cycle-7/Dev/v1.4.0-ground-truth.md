# Cycle-7 ground truth — `v1.4.0` (examiner)

**Classification:** Examiner / Dev only — do not ship in player packets.  
**Plants:** web LFI + compose overlays (see [cycle-7-decisions.md](cycle-7-decisions.md))

---

## Flags

| ID | Value | Location |
|----|--------|----------|
| F1 | `OS{777731571165c37aa74d5385406abb51}` | LFI-readable ops plant file |
| F2 | `OS{0362720305fcd3c72f09b034404b931e}` | Anon FTP loot file |
| F3 | `OS{0060cf7cb47a5ed38b3248f0341b766a}` | Bastion `~/user.txt` (interactive SSH) |
| F4 | `OS{a82695d1063fad40ca2472b6dab29015}` | Bastion `/root/root.txt` after sudo find |
| F5 | `OS{a0cdd819aad20e6eec0fb56134fdb8f0}` | Internal jump host (via tunnel) |

## Intended path

1. **F1:** Auth → `GET /api/ops/documents?path=` traversal to plant → body contains F1.  
2. **F2:** `ftp` anon → `pub/loot.txt` → F2 + `lab` / `labpass`.  
3. **F3:** `ssh -p 2222 lab@$TARGET` → `cat ~/user.txt`. (`:2223` Cowrie is decoy.)  
4. **F4:** `sudo -l` → `find` GTFO → `cat /root/root.txt` (+ `/root/ops-intranet.txt`).  
5. **F5:** Tunnel to `cycle7-jump:8080` with `nwops` / `Ops1ntranet` → F5. Kali must **not** reach jump without pivot.
## Controls that must still hold (tip + Blue)

| Control | Expectation |
|---------|-------------|
| Prod compose alone | No `:21` / `:2222` / `:2223` / jump publish; no `:5433` |
| Preview / bookmarks | Cycle-6 Blue controls remain (no open SSRF / CSRF plant) |
| Notes | XSS remains closed |
| Blue `v2.4.0` | LFI closed; overlays unpublished; plants gone |

## Examiner dry-run (sketch — implement P2)

```bash
docker compose -f infra/docker-compose.prod.yml -f infra/docker-compose.cycle7.yml up -d --build
./infra/assert-pg-unpublished.sh
./infra/smoke-test.sh
./infra/cycle7-examiner.sh
./infra/journey-test.sh
```

## Explicit non-goals

Full AD · Windows VM · docker escape · WordPress · Preview SSRF re-break · Notes XSS · graded Cowrie path.
