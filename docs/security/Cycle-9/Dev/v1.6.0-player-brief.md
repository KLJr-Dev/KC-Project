# Cycle-9 player brief — `v1.6.0`

**Scope:** Intentionally insecure Northwind tip **`v1.6.0`**.  
**Not in scope:** Hardened **`v2.5.0` / `v2.6.0`**; prior-cycle overlay archives unless checked out separately.

Edge note: browser calls **`/api/intake/*`** on the published host — Nest BFF fronts FastAPI (do not expect a direct Intake publish).

## Rules of engagement

- Target: Docker stack from this tag (`docker-compose.prod.yml`).  
- Auth: demo users and/or self-registration are in scope.  
- Report style: AppSec findings with PoC; no shells/AD required.  
- Do not DDoS the lab host.

## Progressive hints (spoiler-light)

1. HR / onboarding workflows often sit behind the Intake API prefix — the edge may be a **gateway/BFF**, not the Python service itself.  
2. Object IDs that look sequential are worth walking.  
3. When two teams share a hop, **identity headers** repay inspection.  
4. Approvals that flip `pending` → `approved` are classic **race** territory.  
5. Export endpoints that take a **filename** invite path tricks (`../`, encodings).  
6. A bolted-on “security events” feed may log more than it should.  
7. Some paths exist to burn time. If a response clearly says **services were alerted**, treat it as a tripwire and move on.

## Deliverables

- Findings table with severity, CWE/OWASP where useful, PoC, impact.  
- Collect graded `OS{…}` proofs if present.  
- Optional: note defence gaps (logging coverage) even when not a flag.

Full solutions: examiner GT after SoftDev; Red writeup on `ctf/v1.6.0` after PenTest freeze.
