# Cycle-9 ground truth — `v1.6.0` (examiner only)

**Spoilers.** Players use [v1.6.0-player-brief.md](v1.6.0-player-brief.md).  
**Status:** Stub — **flag values and exact payloads filled at P2**.

---

## Flags

| ID | Value | Location / how |
|----|-------|----------------|
| F1 | `OS{TBA}` | IDOR `GET /api/intake/onboarding-requests/{foreignId}` |
| F2 | `OS{TBA}` | After header-trust privilege (mod/admin on Intake) |
| F3 | `OS{TBA}` | Approved export + path traversal (`file=../…` / `%2e%2e%2f…`) |
| F4 | `OS{TBA}` | `GET /api/intake/security/events` leak |
| D1 | — | `GET /api/intake/v1/internal/debug` → **services alerted** |

---

## Ledger

| ID | Artifact | Class | Consumed by |
|----|----------|-------|-------------|
| L1 | Seeded onboarding-requests (sequential ids e.g. 9301+) | LIVE | F1 |
| L2 | `X-User-Id` / `X-User-Role` hop trust | LIVE | F2 |
| L3 | Pending request(s) for race | LIVE | G4 → F3 |
| L4 | Export tree + out-of-tree F3 file | LIVE | F3 traversal |
| L5 | Events feed fragment | LIVE | F4 |
| D1 | `/v1/internal/debug` | DECOY | timebox |
| DEMO | Nest demo users | DEMO | G1 |

---

## Exact repro (P2)

1. TBA — IDOR  
2. TBA — header spoof (prefer: BFF forwards client `X-User-*` if present)  
3. TBA — race recipe  
4. TBA — export traversal payloads that must work  
5. TBA — events leak  
6. TBA — honeypot body  

---

## Race + traversal notes (P2)

- Race tool / seed id / success rate: **9302** (`pending`); SoftDev also seeds **9303** pre-`approved` for export-only checks  
- Export base dir: `/app/exports/{id}/` (intended `package.json`)  
- F3 absolute path: `/app/private/onboarding-export.flag` (placeholder `OS{TBA_F3}` until D4)  
- Required encodings: `../`, `%2e%2e%2f`, (optional double-encode)  
- Seed map: **9301** F1 IDOR · **9304** F2 (mod) · **events** F4 placeholder · new POSTs start at **9400+**
