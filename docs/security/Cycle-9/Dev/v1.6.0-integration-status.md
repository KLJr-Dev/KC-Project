# Cycle-9 integration status — `dev`

**Date:** 2026-09-01  
**Branch:** `dev` (backend + frontend merged; triad + examiner green)  
**SoftDev plan:** [v1.6.0-softdev-plan.md](v1.6.0-softdev-plan.md)

## Merged

| Lane | Contents |
|------|----------|
| `backend` | Nest `intake-bff` (JWT + `X-User-*` plant); nginx `/api/intake/` → Nest only; FastAPI onboarding plants (IDOR/race/export/SIEM/honeypot); seed 9301+; F1–F4 flags; `cycle9-examiner.sh`; Nest/Intake `/health` removed; gates use `/api/ping` |
| `frontend` | `lib/api.ts` Intake helpers; `/intake` search+submit; `/intake/queue`; `/admin/security` (`CYCLE9-THEATRE`); nav + tip **`v1.6.0`** |

## Smoke checklist

- [x] `docker compose -f infra/docker-compose.prod.yml up -d --build`
- [x] `/api/ping` 200; `/api/health` and `/api/intake/health` **404** (Nest BFF edge; no nginx→Intake direct)
- [x] `./infra/smoke-test.sh` (C6/C7/C8 Blue + unpublished asserts)
- [x] `./infra/journey-test.sh`
- [x] `./infra/e2e-docker.sh` (**166** / 166)
- [x] `./infra/cycle6-blue-assert.sh` · `./infra/cycle7-blue-assert.sh` · `./infra/cycle8-blue-assert.sh`
- [x] `./infra/cycle9-examiner.sh` (F1–F4 + honeypot; ping not `/health`)
- [x] Tip UI **`v1.6.0`** / Northwind (homepage + footer)
- [x] `./infra/assert-pg-unpublished.sh` · `./infra/assert-cycle8-unpublished.sh`

## Next

P6 — Red writeup on **`ctf/v1.6.0`** → freeze → Blue handoff → **`remediation/v2.6.0`**.
