# Cycle-9 — v2.5.0 gap closure map (`v2.6.0` target ~80%)

**Purpose:** Map every major “v2.5.0 not secure” bucket to Cycle-9 role: Red fuel, graded plant, Wave B Blue, residual, or Cycle-10+.  
**Authority:** [cycle-9-decisions.md](cycle-9-decisions.md) · [ADR-038](../../../decisions/ADR-038-cycle-9-onboarding-defence-pair.md)

**Legend**

| Role | Meaning |
|------|---------|
| **Recon** | Usable on insecure tip; not a graded “new Critical” |
| **Plant** | Graded on `v1.6.0`; closed Wave A |
| **Wave B** | Closed on `v2.6.0` without being the Red story |
| **Residual** | Accepted on `v2.6.0` (lab) |
| **C10+** | Deferred to `v2.10.0` / Cycle-11+ |

---

## 1. Transport & TLS

| Item | Role | Notes |
|------|------|-------|
| HTTP `:8080` default | Residual | C8-R01 lineage; not graded |
| No HSTS on HTTP profile | Residual | Comes with TLS overlay |
| `COOKIE_SECURE=false` on HTTP | Residual | |
| TLS overlay optional | Residual / C10 | Flip TLS-default → C10 |
| Self-signed lab certs | Residual | |
| TLS 1.2 enabled | C10 | Optional harden later |
| Passwords over HTTP | Residual | Lab profile |
| nginx `0.0.0.0` | Residual / optional Wave B | Prefer residual; bind `127.0.0.1` is C10 polish |

## 2. Intake microservice

| Item | Role | Notes |
|------|------|-------|
| No API authentication | **Plant** (evolves) | Edge becomes Nest BFF; Intake trusts `X-User-*`; Blue = RS256 in Intake |
| nginx → Intake direct | **Rewire** | SoftDev: `/api/intake/` → Nest only (topology fix + plant host) |
| No nginx rate limit `/api/intake/` | Wave B | |
| Runs as DB admin | Wave B | `kc_intake_app` |
| `/health` unauthenticated | Recon | **Removed** Cycle-9 SoftDev — use `/api/ping` |
| Staff directory enumerable | Recon | Keep search; no SQLi |
| C8-R07 architecture | Keep | Hardened + auth’d after Blue |

## 3. Authentication & session

| Item | Role | Notes |
|------|------|-------|
| Open self-registration | Residual | |
| Predictable demo passwords | Residual | |
| Lab UI quick-fill | Residual | |
| Demo creds in docs | Residual | |
| Access JWT in JS memory | Residual | CSP weak residual |
| CORS credentials | Residual | |
| Thin auth rate limit | Recon / C10 | Optional Wave B tune; not headline |
| Refresh TOCTOU | Out | Race is cases only |

## 4. Application logic

| Item | Role | Notes |
|------|------|-------|
| Sequential entity IDs | Recon | Aid IDOR; residual after Blue |
| Preview snippets | Residual | C6-R02 |
| Ops handbook any authed | Residual | C7-R06 |
| Swagger env-toggle | Recon | |
| No global nginx DoS limits | Wave B (light) | Connection/request zones |
| Uploads volume / 10 MB | Residual / C10 | AV = C10 |
| Audit admin-only | Recon | Extend with SIEM plant |

## 5. Secrets & credentials

| Item | Role | Notes |
|------|------|-------|
| Default compose passwords | Wave B | Assert script |
| Secrets in env vars | Residual / C10 | Docker Secrets → C10 |
| JWT keys in repo | Residual | Lab |
| Intake superuser DB | Wave B | |
| `.env.example` not enforced | Wave B | Assert defaults fail CI/smoke |

## 6. Infrastructure & containers

| Item | Role | Notes |
|------|------|-------|
| Containers as root | Wave B | `USER 1001` backend+intake |
| No read-only root FS | C10 | |
| No `internal: true` network | Wave B | |
| Only mem_limit | Wave B | Add CPU where easy |
| nginx version leakage | Wave B | `server_tokens off` |
| Postgres superuser for migrations | Residual | App/Intake least-priv is enough |
| No cert rotation / SBOM | C10 | |

## 7. Security headers & browser

| Item | Role | Notes |
|------|------|-------|
| CSP unsafe-inline/eval | Residual | Next tradeoff; strict → C10 |
| Referrer-Policy | OK | |
| Missing COOP/COEP/CORP | C10 | |
| No HSTS on HTTP | Residual | |

## 8. Optional overlays

| Item | Role | Notes |
|------|------|-------|
| e2e / lab-host overlays | Residual | Policy unchanged |
| cycle8 archive | Residual | Don’t deploy wrong ref |

## 9. Documented accepted residuals (prior)

Stay residual unless Wave B explicitly closes them. Cycle-9 **adds** new residuals only via §F of decisions (Pile D).

## 10. Baseline aspirational (security-baseline.md)

| Control | Role |
|---------|------|
| TLS-only external | C10 |
| Non-root | Wave B |
| Read-only FS | C10 |
| Docker Secrets | C10 |
| Internal DB net | Wave B |
| Strict CSP | C10 |
| Global nginx limits | Wave B (light) |
| Structured logging + request ID | Wave B (+ plant close) |
| Demo strip from prod builds | C10 |
| Baseline doc tip version | Wave B docs pass (`v2.6.0`) |

---

## Scorecard (planning)

| Bucket | Approx count | Cycle-9 disposition |
|--------|--------------|---------------------|
| Closed Wave A (plants) | 4–5 findings | Required |
| Closed Wave B | ~12–15 items | Required for `v2.6.0` secure-ready |
| Residual lab | ~10–12 | Documented |
| Deferred C10 / C11+ | ~10–15 | Explicit |

**Target claim at `v2.6.0` secure-ready:** *~80% of the absolute-insecurity inventory addressed or consciously residual; remaining production controls reserved for `v2.10.0`.*

---

## Wave B verification (target scripts)

| Script | Asserts |
|--------|---------|
| `cycle9-blue-assert.sh` | Plants closed (IDOR 403, header spoof fail, race fail, events redacted) |
| `assert-no-default-secrets.sh` | Compose/.env defaults rejected in gate profile |
| Existing | `assert-pg-unpublished`, cycle6–8 blue asserts stay green |
| Smoke | Request-ID present; intake rate limit 429 under burst; containers non-root (`docker inspect`) |
