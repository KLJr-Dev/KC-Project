# Cycle-4 box plan — `v1.2.0` (Notes + SSH foothold)

**Status:** Decisions locked · [v1.2.0-decisions.md](v1.2.0-decisions.md)  
**Execution:** [v1.2.0-execution-plan.md](v1.2.0-execution-plan.md)  
**ADR:** [ADR-033](../../../decisions/ADR-033-cycle-4-softdev-version-pair.md)  
**Consumes:** FC-01, FC-14 (SSH foothold)  
**Next depth:** Cycle-5 / **`v1.3.0`** shells + PrivEsc  

---

## One-liner

Notes SoftDev (RBAC + search + XSS + optional attachment) → logical SSH → foothold loot → stop. **No** PrivEsc. **No** published Postgres.

## Architecture

```text
main @ v1.2.0 (intentional insecure SoftDev)
  Notes: user CRUD | mod: view+flag | admin: view+delete
  XSS: HTML + unsafe markdown; search parameterized
  Plant: admin ops note (SSH) + decoys; XSS primary path
  compose: prod.yml + docker-compose.ssh.yml
       → :2222 lab@… → user.txt OS{…} + .bak loot
```

## Flag format

`OS{` + 32 lowercase hex + `}` — checkpoints F1 (XSS), F2 (priv note), F3 (SSH). See decisions.

## Ceiling

Foothold only. `/opt/kc-lab/README` tease for `v1.3.0` — no live PrivEsc.

## Non-goals

PrivEsc · published PG · Notes SQLi · SSH baked into default prod without overlay · FTP  
