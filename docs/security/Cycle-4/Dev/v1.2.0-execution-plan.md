# Cycle-4 execution plan — build `v1.2.0`

**Status:** SoftDev **shipped** (`v1.2.0`) · Red open · decisions locked in [v1.2.0-decisions.md](v1.2.0-decisions.md)  
**Box:** [v1.2.0-box-plan.md](v1.2.0-box-plan.md) · **ADR-033**

---

## Pattern (C1 SoftDev + gate culture)

```text
SoftDev (backend/frontend → dev → main)   # intentional insecure DLC on main
  → prod + ssh overlay for full chain
  → v1.2.0-pentest-ready → tag v1.2.0 on main
  → archive ctf/v1.2.0 from tag (Red; don’t Blue-fix)
  → Red writeup (F1–F3, stop at foothold)
  → remediation/v2.2.0 → tag v2.2.0 (Notes hardened; no default SSH)
```

Unlike Cycle-2/3, **`v1.2.0` lives on `main`** (your §11–12). Overlay still isolates SSH.

---

## Definition of Done (`v1.2.0`)

- [x] Notes CRUD + search + mod flag + admin delete  
- [x] XSS sinks (HTML + MD) demonstrated in GT  
- [x] Admin ops note + decoy notes seeded  
- [x] Optional note attachment (if not cut)  
- [x] SSH overlay: user `lab`, F3 in `~/user.txt`, `.bak` loot  
- [x] Flags `OS{32hex}` for F1–F3  
- [x] Progressive hints in player brief  
- [x] No published PG; no PrivEsc  
- [x] Examiner dry-run green  
- [x] `docs/release/v1.2.0-pentest-ready.md` signed  
- [x] Tag `v1.2.0` · `ctf/v1.2.0` archive  

---

## Workstreams

### W0 — Docs
Commit Cycle-4/5 + decisions; mark FC-01/FC-14 in progress.

### W1 — Backend Notes
Entity (`title`, `body`, `flagged`, `userId`, optional attachment path) · CRUD · list scoping by role · mod flag · admin delete · **parameterized** search · seeds (admin SSH note + decoys + flag plants).

### W2 — Frontend Notes
Product `/notes` · mod/admin queues/views · HTML + unsafe MD render · search UI · upload attachment UI.

### W3 — SSH
`infra/docker-compose.ssh.yml` · user `lab` · home loot + `.bak` · light `/opt/kc-lab/README` · `assert-ssh-unpublished.sh` · examiner script.

### W4 — Dev triad + gate
Player brief (hint levels) · ground truth · `v1.2.0-pentest-ready.md` · PenTest scope stub.

### W5 — Ship
Merge SoftDev → `main` · tag · archive branch · Red.

### W6 — Blue (later)
`remediation/v2.2.0`: sanitize render, MIME lockdown, no SSH default, keep Notes+RBAC+search.

---

## Must-ship plants

| ID | Change |
|----|--------|
| P1 | Notes + tertiary RBAC |
| P2 | Search (parameterized) |
| P3 | HTML + MD XSS sinks |
| P4 | Admin ops note (real SSH) + decoy notes |
| P5 | SSH overlay + `lab` + F3 |
| P6 | `.bak` / home loot |
| P7 | F1/F2 plants (`OS{…}`) |
| P8 | Examiner + pentest-ready |

### Cut if needed
Note attachments (P-attach) — only schedule slip valve.

### Must not
PrivEsc · `:5433` · Notes SQLi · XSS-free “secure” merge without Blue tag story.

---

## Immediate next

1. ~~Confirm soft defaults~~ → **locked**  
2. ~~Commit planning docs to `main`~~  
3. ~~Reset SoftDev rails (`backend` / `frontend` / `dev`)~~ → **P0 done** ([softdev-status](v1.2.0-softdev-status.md))  
4. **P1a on `backend`:** Notes entity + owner CRUD + migration + e2e  
