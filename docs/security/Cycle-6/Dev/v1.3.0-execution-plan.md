# Cycle-6 execution plan — build `v1.3.0`

**Status:** P0 design · decisions [cycle-6-decisions.md](cycle-6-decisions.md) · box [v1.3.0-box-plan.md](v1.3.0-box-plan.md) · [ADR-034](../../../decisions/ADR-034-cycle-6-product-expansion-pair.md)  
**Docs work:** branch **`docs/cycle-6-p0`** (PR → `main`). Product code: feature lanes after P0 merges — **never develop P0/P2 on `main` tip directly.**

---

## Pattern (product expansion + ADR-015)

```text
docs/cycle-6-p0 → PR → main          # P0 design only
  → reset backend / frontend / dev from main
  → implement on backend + frontend → merge → dev → PR → main
  → triad + v1.3.0-pentest-ready → tag v1.3.0
  → archive ctf/v1.3.0
  → Red (Socratic)
  → remediation/v2.3.0 → tag v2.3.0
```

---

## Definition of Done (`v1.3.0`)

- [ ] Link Preview API + UI (open server fetch)  
- [ ] CSRF-gap mutation + UI path that exercises it  
- [ ] Two `OS{32hex}` plants in GT  
- [ ] Tip UI version strings show `v1.3.0` (not stale `v2.0.0`)  
- [ ] Player brief · ground truth · examiner · `docs/release/v1.3.0-pentest-ready.md` signed  
- [ ] Tag `v1.3.0` · `ctf/v1.3.0` archive  
- [ ] No `:5433` on prod alone · no Notes XSS re-break · no PrivEsc  

---

## Phases

### P0 — Requirements & threat design (this branch)

- [x] Cycle-6 hub + Dev decisions / box / execution  
- [x] ADR-034 + glossary preferred terms  
- [x] STRIDE draft (SSRF + CSRF)  
- [x] Indexes / roadmap tip: C5 closed; C6 next  
- [x] FC-02/03 in progress; OSCP-inspired FC-17/18 later  
- [x] UI version string inventory (fix on frontend lane)  
- [ ] PR `docs/cycle-6-p0` → `main`  

**Exit:** Design on `main` via PR. No insecure product code yet.

### P1 — Reset feature lanes

From updated `main`: force-reset `backend` / `frontend` / `dev` (ADR-015 SoftDev/cycle start). Work continues on those branches — not on `main`.

### P2 — Backend (`backend`)

Link Preview fetch · CSRF-gap route · seeds/flags · tests.

### P3 — Frontend (`frontend`)

Preview UI · CSRF-relevant flow · version strings → `v1.3.0`.

### P4 — Integrate (`dev`)

Merge lanes → smoke / journey / e2e → `v1.3.0-integration-status.md`.

### P5 — Release insecure tip

PR `dev` → `main` → triad + pentest-ready → tag **`v1.3.0`** → **`ctf/v1.3.0`**.

### P6 — PenTest

Socratic Red; writeup on `ctf/v1.3.0`; freeze.

### P7 — Remediation

`remediation/v2.3.0` → harden → tag **`v2.3.0`** → tip UI `v2.3.0`.

---

## Must-ship plants

| ID | Change |
|----|--------|
| P-ssrf-1 | Open URL fetch / preview API |
| P-ssrf-2 | Minimal UI |
| P-csrf-1 | One cookie mutation without CSRF |
| P-flag-* | Two `OS{32hex}` |
| P-doc-1 | Brief · GT · examiner · pentest-ready |
| P-ui-ver | Landing/footer version accurate |

### Must not

PrivEsc · FTP · Notes XSS re-break · `:5433` · AD · spoon-feed Red · push cycle work straight to `main` without PR/lanes.

---

## Immediate next

1. Open PR: `docs/cycle-6-p0` → `main`.  
2. After merge: **P1** reset feature lanes.  
3. Grill P2: exact CSRF route + SSRF prize seed.
