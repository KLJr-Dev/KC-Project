# Cycle-6 integration status — `dev`

**Date:** 2026-08-26  
**Branch:** `dev` @ `794433a` (backend `ca51f61` + frontend `bc264b0`)

## Merged

| Lane | Commit | Contents |
|------|--------|----------|
| `backend` | `ca51f61` | `PreviewModule`, open fetch, `/internal/cycle6-flag`, cookie `/auth/bookmarks*` CSRF gap, migration `link_bookmark` |
| `frontend` | `bc264b0` | `/preview` UI, API helpers, tip strings **v1.3.0** |

## Smoke checklist (before PR → `main`)

- [ ] `docker compose` prod up; migrations apply (`link_bookmark`)
- [ ] Login → `/preview` → fetch `https://example.com`
- [ ] Preview `http://127.0.0.1:4000/internal/cycle6-flag` (or in-compose loopback) → see F1 in snippet
- [ ] Save bookmark (cookie, no CSRF header) → F2 `proof` in JSON / list
- [ ] `POST /auth/refresh` still requires `X-Requested-With`
- [ ] Notes XSS still closed; no `:5433` on prod alone

## Next

P5: PR `dev` → `main` → triad + `v1.3.0-pentest-ready` → tag → `ctf/v1.3.0`.
