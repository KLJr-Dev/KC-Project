# Cycle-6 integration status — `dev`

**Date:** 2026-08-26  
**Branch:** `dev` (backend + frontend merged; synced with `main` chore)

## Merged

| Lane | Contents |
|------|----------|
| `backend` | `PreviewModule`, open fetch, `/internal/cycle6-flag`, cookie `/auth/bookmarks*` CSRF gap, migration `link_bookmark` |
| `frontend` | `/preview` UI, API helpers, tip strings **v1.3.0** |

## Smoke checklist

- [x] `docker compose -f infra/docker-compose.prod.yml up -d --build`
- [x] `./infra/smoke-test.sh`
- [x] `./infra/journey-test.sh`
- [x] `./infra/cycle6-examiner.sh` — F1 SSRF + F2 CSRF; refresh 403 without CSRF
- [x] Asserts: no PG `:5433` / SSH `:2222` / `:8787` on prod compose alone

## Next

P5: PR `dev` → `main` → sign pentest-ready → tag `v1.3.0` → `ctf/v1.3.0`.
