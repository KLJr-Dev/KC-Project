# Remediation — Cycle-6

Stub until after Red. Blue branch: **`remediation/v2.3.0`** → tag **`v2.3.0`**.

Target controls: fetch allowlist / block loopback & link-local & IMDS; CSRF restored on planted mutation; tip UI version `v2.3.0`.
