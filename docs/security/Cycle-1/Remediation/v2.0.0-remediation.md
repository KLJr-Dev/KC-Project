# Remediation Report — KC-Project v2.0.0

**Cycle:** 1 · **Source pentest:** [v1.0.0-writeup.md](../PenTest/v1.0.0-writeup.md) · **Baseline:** [security-baseline.md](../../../spec/security-baseline.md)  
**Ground truth (before):** [v1.0.0-ground-truth.md](../Dev/v1.0.0-ground-truth.md)  
**Status:** **Blue Team active** on branch `remediation/v2.0.0` — map complete; implementation per [blue-team-plan.md](blue-team-plan.md).  
**Pentest evidence branch:** `pentest/cycle-1` (do not rewrite vuln history there).

---

## Summary

| Metric | Value |
|--------|-------|
| Findings from v1.0.0 pentest | **13** (F-01 … F-13) |
| Confirmed Critical / High | 3 Critical · 3 High |
| In scope for v2.0.0 secure parallel | All 13 (map to [security-baseline.md](../../../spec/security-baseline.md)) |
| Implementation started | **Yes** — planning on `remediation/v2.0.0`; code Wave A next |
| Deferred to later product / CTF cycles | Dense “kitchen-sink” surface redesign → **v1.1.0 CTF** (fewer chained vulns); not a substitute for v2.0.0 fixes |
| Residual risk (after v2.0.0) | Lab demo accounts may remain in non-prod builds; accept or gate behind `NODE_ENV` |

**Handoff verdict:** Offensive deliverable is complete enough to drive remediation. Optional leftover (moderator approve forge) does **not** block Blue Team — covered by F-03 / baseline RBAC.

**Branch plan:**

1. `pentest/cycle-1` — frozen evidence / writeup (**done**).
2. `remediation/v2.0.0` — Blue Team plan + secure parallel implementation (**current**).
3. Implement waves in [blue-team-plan.md](blue-team-plan.md); flip e2e from “expect vuln” → “expect deny”.
4. Tag `v2.0.0` when baseline checklist + e2e green.
5. Later: **v1.1.0** = new intentionally vulnerable CTF surface (narrower), not “un-fix v2”.

---

## Remediation matrix

| ID | CWE | v1.0.0 weakness | v2.0.0 fix (intent) | Primary files | Verification | Residual |
|----|-----|-----------------|---------------------|---------------|--------------|----------|
| F-01 | 200 | Open `/api/docs` + offensive annotations | Disable Swagger outside `development`; strip CWE attack hints from public descriptions | `backend/src/main.ts` | Unauth `GET /api/docs` → 404/401 in prod compose | Internal docs OK in private envs |
| F-02 | 639 | File IDOR (metadata + download) | Enforce `ownerId === caller` (or share ACL) on get/download/delete/list | `backend/src/files/files.service.ts`, `files.controller.ts` | User JWT → `/files/9104` **403**; own file 200 | Admin/mod override if product needs it — explicit |
| F-03 | 347 / 639 / 798 | HS256 `kc-secret`; trust JWT `role` | Env/secret-managed keys; prefer **RS256**; **HasRoleGuard loads role from DB** | `auth.module.ts`, `auth.service.ts`, `guards/has-role.guard.ts`, `jwt-auth.guard.ts` | Forged `role:admin` → **403**; real admin still 200 | Key rotation runbook |
| F-04 | 285 / 330 | `share-N` tokens; unauth public GET | `crypto.randomBytes`; enforce `expiresAt`; rate-limit public route | `sharing.service.ts`, `sharing.controller.ts`, seed migrations | `share-1` style guess fails; only unguessable token works | Optional auth-gated shares |
| F-05 | 798 | Postgres on host + defaults | Do not publish `5433` in prod compose; strong password via secrets; no default in docs for prod | `infra/docker-compose.prod.yml`, `infra/.env.example` | Host nmap: **5433 closed**; app still reaches DB internally | Dev compose may keep host port — document |
| F-06 | 200 | Missing headers / stack disclosure | nginx security headers; hide `X-Powered-By` | `infra/nginx.conf`, Next/Nest config | Headers present on `/` and `/api` | — |
| F-07 | 942 | CORS `*` | Explicit origin allowlist | `backend/src/main.ts` | Disallowed origin blocked | Localhost origins for lab |
| F-08 | 204 | Distinct login errors | Generic `"Authentication failed"` | `auth.service.ts`, `auth.controller.ts` | Same body for unknown email vs bad password | Timing side-channels later |
| F-09 | 613 | No `exp`; logout no-op | `expiresIn` on access token; refresh store; logout revokes | `auth.module.ts`, `auth.service.ts`, `auth.controller.ts` (+ new refresh entity/migration) | Expired token 401; logout invalidates refresh | Cookie migration (baseline: httpOnly) can be same wave or follow-up |
| F-10 | 862 / 284 | audit-logs + DELETE missing admin role | `@HasRole('admin')` on both; deny-by-default audit | `admin.controller.ts` | User JWT → audit-logs **403**; DELETE **403** | Soft-delete / cascade orphans |
| F-11 | 200 / 319 / 615 | Demo passwords in UI/JS; HTTP | Strip `DEMO_USERS` from prod builds; TLS in real deploy | `frontend/lib/demo-users.ts`, `app/page.tsx`, `app/auth/page.tsx`, nginx TLS | Prod bundle has no plaintext demo passwords | Keep demo UX only when `NEXT_PUBLIC_DEMO=1` |
| F-12 | 200 | Anonymous `/dev` | Gate `/dev` behind auth or `NODE_ENV===development` | `frontend/app/dev/**`, `footer.tsx`, `app/page.tsx` | Prod: `/dev` 404 or redirect login | — |
| F-13 | 209 | Version / CWE strings in errors | Stable client errors; details in server logs only | auth/error filters, controller messages | 401/403 bodies lack `v0.1.x` / CWE ids | — |

---

## Implementation waves (suggested order)

Do not boil the ocean in one PR. Wave order matches exploit impact.

### Wave A — P0 authz (stops IDOR + admin wipe + JWT admin)

| Work | Files | Closes |
|------|-------|--------|
| Ownership checks on file read/download/delete (+ list scoped) | `backend/src/files/files.service.ts`, `files.controller.ts` | F-02 |
| DB role re-validation in `HasRoleGuard` | `backend/src/auth/guards/has-role.guard.ts` (+ UsersService/repo inject) | F-03 (partial), baseline RBAC |
| `@HasRole('admin')` on `DELETE users/:id` and `GET audit-logs` | `backend/src/admin/admin.controller.ts` | F-10 |
| Remove / replace hardcoded `kc-secret`; `expiresIn` | `backend/src/auth/auth.module.ts`, `auth.service.ts`, env | F-03, F-09 (exp) |

**Verify:** re-run writeup PoCs — expect **deny**. Update `backend/test/rbac.e2e-spec.ts`, `inconsistency.e2e-spec.ts`, `files.e2e-spec.ts` accordingly.

### Wave B — P0 infra + shares

| Work | Files | Closes |
|------|-------|--------|
| Unpublish Postgres port; strong `DB_PASSWORD` | `infra/docker-compose.prod.yml`, `.env.example`, secrets docs | F-05 |
| Random share tokens + expiry check | `backend/src/sharing/sharing.service.ts`, `sharing.controller.ts`, seeds | F-04 |

### Wave C — P1 disclosure surface

| Work | Files | Closes |
|------|-------|--------|
| Swagger only in non-prod | `backend/src/main.ts` | F-01 |
| Remove/gate `/dev` + demo passwords in prod | `frontend/app/dev/**`, `lib/demo-users.ts`, landing/auth | F-11, F-12 |
| Generic auth errors; sanitize error bodies | `auth.service.ts`, global filter if present | F-08, F-13 |

### Wave D — P2 baseline polish

| Work | Files | Closes |
|------|-------|--------|
| CORS allowlist | `backend/src/main.ts` | F-07 |
| nginx headers (+ TLS story for real deploy) | `infra/nginx.conf` | F-06, F-11 transport |
| Logout revocation + refresh tokens | auth module + migration | F-09 complete |
| MIME magic bytes / path sanitize (baseline; light in Cycle-1 pentest) | `files.service.ts` / multer config | baseline file controls |

---

## Control groups (from security-baseline)

### Authentication

| Control | Implementation plan | Finding IDs | Verified |
|---------|---------------------|-------------|----------|
| bcrypt cost 12+ | Confirm / bump in `auth.service.ts` register/login path | (inventory) | [ ] |
| RS256 JWT (or strong env HS256 interim) | `JwtModule.register` → keys from env/files | F-03 | [ ] |
| Token expiration | `signAsync` / module `expiresIn: '15m'` | F-09 | [ ] |
| Generic auth errors | Single failure message | F-08 | [ ] |
| Logout revokes session | Persist refresh; delete on logout | F-09 | [ ] |

### Authorization

| Control | Implementation plan | Finding IDs | Verified |
|---------|---------------------|-------------|----------|
| DB role re-validation | `HasRoleGuard` queries `User.role` by `sub` | F-03 | [ ] |
| Consistent HasRole on admin routes | Decorate DELETE + audit-logs | F-10 | [ ] |
| Ownership on resources | Files (+ sharing mutate) | F-02 | [ ] |

### File / sharing

| Control | Implementation plan | Finding IDs | Verified |
|---------|---------------------|-------------|----------|
| Ownership checks | Service-layer assert | F-02 | [ ] |
| Unguessable tokens + expiry | `randomBytes` + `expiresAt` gate | F-04 | [ ] |
| Filename / MIME (baseline) | sanitize + magic bytes | (deferred depth) | [ ] |

### Infrastructure / client

| Control | Implementation plan | Finding IDs | Verified |
|---------|---------------------|-------------|----------|
| DB not on host; secrets | compose + Docker secrets | F-05 | [ ] |
| nginx headers / TLS | `nginx.conf` | F-06 | [ ] |
| CORS allowlist | `enableCors({ origin: […] })` | F-07 | [ ] |
| No prod demo/`/dev` | build flags | F-11, F-12 | [ ] |
| Swagger gated | `main.ts` | F-01 | [ ] |

---

## Per-finding detail

### F-01 — Unauthenticated Swagger

**v1.0.0 state:** `/api/docs` + `/api/docs-json` open; descriptions advertise CWEs.

**v2.0.0 change:** Register Swagger only when `NODE_ENV !== 'production'` (or `ENABLE_SWAGGER=true`). Neutral operation summaries.

**Files:** `backend/src/main.ts` (DocumentBuilder / `SwaggerModule.setup`).

**Verification:**

```bash
curl -sS -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/docs
# expect 404 (or 401) on prod stack
```

---

### F-02 — Horizontal IDOR (files)

**v1.0.0 state:** Any JWT reads `/api/files/9104` + download.

**v2.0.0 change:** Every get/download/delete checks `ownerId === req.user.sub` (admins may bypass via explicit role check). List endpoint returns only owned files (or paginated admin view).

**Files:** `backend/src/files/files.service.ts`, `files.controller.ts`; DTOs if `storagePath` still leaked (baseline CWE-200).

**Verification:**

```bash
# user token → 403
curl -sS -w "\n%{http_code}\n" -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:8080/api/files/9104
```

---

### F-03 — JWT role forgery

**v1.0.0 state:** HS256 `kc-secret`; guard trusts claim.

**v2.0.0 change:**
1. Secret/keys from environment (no repo constant).
2. Prefer RS256 per baseline.
3. `HasRoleGuard`: load user by `sub`, compare **DB** role to `@HasRole` metadata.

**Files:** `backend/src/auth/auth.module.ts`, `auth.service.ts`, `guards/has-role.guard.ts`, `jwt-auth.guard.ts`, `jwt-payload.interface.ts`.

**Verification:** Forge `role:admin` with old/new secret → admin routes **403** unless DB role is admin.

---

### F-04 — Public share tokens

**v1.0.0 state:** `share-${id}`; unauth GET returns bytes; expiry ignored.

**v2.0.0 change:** Random token; check `expiresAt`; rate-limit; update seeds/migrations away from `share-1`.

**Files:** `backend/src/sharing/sharing.service.ts`, `sharing.controller.ts`, `migrations/*SeedDemo*`.

---

### F-05 — Exposed PostgreSQL

**v1.0.0 state:** `5433:5432`, defaults `postgres`/`postgres`.

**v2.0.0 change:** Remove host port mapping from prod compose (backend uses service name `postgres:5432`). Strong password via secret/env; document that missing `infra/.env` must not silently use lab defaults in “secure” profile.

**Files:** `infra/docker-compose.prod.yml`, `infra/.env.example`, deploy docs.

**Verification:** `nmap` host — 5433 closed; app health still OK.

---

### F-06 / F-07 — Headers & CORS

**Files:** `infra/nginx.conf`; `backend/src/main.ts` (`enableCors`).

---

### F-08 / F-13 — Auth & error disclosure

**Files:** `backend/src/auth/auth.service.ts` (login messages); global exception filter if any; strip version tags from guard messages (`jwt-auth.guard.ts`, `has-role.guard.ts`).

---

### F-09 — Session lifetime / logout

**Files:** `auth.module.ts`, `auth.service.ts`, `auth.controller.ts`; new TypeORM entity + migration for refresh tokens (per baseline).

---

### F-10 — Admin authz gaps

**v1.0.0 state:** User JWT → audit-logs 200; DELETE user 204.

**v2.0.0 change:** `@HasRole('admin')` on both handlers; ensure `HasRoleGuard` runs (already on controller class — metadata was the bug for DELETE).

**Files:** `backend/src/admin/admin.controller.ts` (+ soft-delete/cascade in `admin.service.ts` if present).

**Verification:**

```bash
curl -sS -w "%{http_code}\n" -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:8080/api/admin/audit-logs
# 403

curl -sS -o /dev/null -w "%{http_code}\n" -X DELETE \
  -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:8080/api/admin/users/11
# 403
```

---

### F-11 / F-12 — Demo UX & `/dev`

**Files:** `frontend/lib/demo-users.ts`, `frontend/app/page.tsx`, `frontend/app/auth/page.tsx`, `frontend/app/dev/**`, `frontend/app/components/footer.tsx`.

**v2.0.0 change:** Compile out or feature-flag demo passwords and `/dev` for production builds.

---

## e2e / test debt (must flip with code)

Intentionally “vuln-expecting” tests today — update expectations on the harden branch:

| Suite | Today encodes | v2.0.0 expectation |
|-------|---------------|-------------------|
| `backend/test/rbac.e2e-spec.ts` | Forged admin works | Forge fails |
| `backend/test/inconsistency.e2e-spec.ts` | DELETE without admin works | 403 |
| `backend/test/files.e2e-spec.ts` | IDOR / `share-N` patterns | Ownership + random tokens |
| `backend/test/escalation.e2e-spec.ts` | Escalation paths | Hardened rules |
| `infra/e2e-docker.sh` / journey | Baseline journeys | Secure journeys + negative tests |

---

## Sign-off

- [ ] All Cycle-1 findings addressed or explicitly accepted
- [ ] security-baseline.md controls verified
- [ ] e2e suite green on v2.0.0 branch (secure expectations)
- [ ] Ground truth updated / fork note for next cycle
- [ ] Pentest writeup left intact as v1.0.0 evidence (do not “fix” history on `pentest/cycle-1`)

---

## Immediate next actions

1. Follow [blue-team-plan.md](blue-team-plan.md) — start **Wave A1/A2**.
2. After each wave, tick verification commands in this matrix.
3. Park **v1.1.0 CTF** design until Wave A–B land.
