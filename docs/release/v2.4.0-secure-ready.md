# v2.4.0 — Secure-Ready Declaration (Cycle-7 Blue)

Formal gate that the **secure parallel** after Cycle-7 Red is ready to tag as `v2.4.0`.

**Status:** **Signed** (2026-08-27) — Waves A–D complete on `remediation/v2.4.0`.  
**ADR:** [ADR-035](../decisions/ADR-035-cycle-7-multi-service-pair.md) · Plan: [blue-team-plan.md](../security/Cycle-7/Remediation/blue-team-plan.md)  
**Map:** [v2.4.0-remediation.md](../security/Cycle-7/Remediation/v2.4.0-remediation.md)  
**Residuals:** [accepted-residuals.md](../security/Cycle-7/Remediation/accepted-residuals.md)  
**Red evidence:** tag / branch `v1.4.0` / `ctf/v1.4.0` (frozen)

> Gate signed after Waves A–D + smoke (incl. `cycle6-blue-assert` + `cycle7-blue-assert` + `assert-cycle7-unpublished`) + journey + **tls-smoke** (2026-08-27).

---

## Product intent (delta vs `v1.4.0`)

| Domain | Secure `v2.4.0` |
|--------|-----------------|
| Ops Documents | Kept — authenticated handbook read under library root |
| LFI / traversal | Closed — path confined; no F1 plant |
| Overlays | Unpublished on default prod (`:21` / `:2222` / `:2223` / jump) |
| Plants | No F1–F5 `OS{` on tip |
| Prior Blue | Preview SSRF + bookmark CSRF + Notes XSS remain closed |
| SSDLC | TLS profile required at this gate (inherit Cycle-6) |

---

## Deployment (verify on)

**Day-to-day HTTP (loopback residual C7-R01):**

```bash
docker compose -f infra/docker-compose.prod.yml up -d --build
./infra/assert-cycle7-unpublished.sh
./infra/assert-pg-unpublished.sh
./infra/smoke-test.sh && ./infra/journey-test.sh && ./infra/e2e-docker.sh
```

**Pre-tag TLS profile (required):**

```bash
./infra/scripts/gen-lab-certs.sh
docker compose -f infra/docker-compose.prod.yml -f infra/docker-compose.tls.yml up -d --build
./infra/tls-smoke.sh
```

**LAN / recruiter secure demo:** use the TLS profile. Do not advertise cleartext `:8080` on a reachable NIC as “secure.”  
**Insecure Cycle-7 replay:** pin tag/`ctf/v1.4.0` + `docker-compose.cycle7.yml` — not this tip.

---

## Findings — secure expectation

| ID | Secure expectation | Status |
|----|--------------------|--------|
| C7-F01 | Ops path confined; no F1 / traversal plant on tip | **Verified** |
| C7-F02 | No anon FTP on default prod tip | **Verified** |
| C7-F03 | No bastion publish / tip SSH plant | **Verified** |
| C7-F04 | No sudo-find shell plant on tip | **Verified** |
| C7-F05 | No jump / intranet plant on tip | **Verified** |
| C7-F06 | Dual SSH / Cowrie archive-only | **Accepted residual** |
| Prior | Preview / CSRF / Notes stay closed | **Verified** (smoke / cycle6 asserts) |
| SSDLC-TLS | tls-smoke green on TLS profile | **Verified** |

---

## Gate checklist

- [x] Wave A–C code complete on this branch
- [x] LFI blue negative assert green (`./infra/cycle7-blue-assert.sh` via smoke)
- [x] `./infra/assert-cycle7-unpublished.sh`
- [x] `./infra/assert-pg-unpublished.sh`
- [x] `./infra/smoke-test.sh`
- [x] `./infra/journey-test.sh`
- [ ] `./infra/e2e-docker.sh` (run before merge if CI/local allows)
- [x] `./infra/tls-smoke.sh` (TLS profile)
- [x] Tip UI / version shows `v2.4.0`
- [x] Bucket B FC-14 (FTP) / FC-18 marked Consumed (Blue closed)
- [x] Sign-off below

**Signed:** Cycle-7 Blue (portfolio)  
**Date:** 2026-08-27
