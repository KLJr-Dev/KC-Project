# v2.5.0 — Secure-Ready Declaration (Cycle-8 Blue)

Formal gate that the **secure parallel** after Cycle-8 Red is ready to tag as `v2.5.0`.

**Status:** **Signed** (2026-08-28) — Waves A–D complete on `remediation/v2.5.0`.  
**ADR:** [ADR-036](../decisions/ADR-036-cycle-8-intake-tool-chain-pair.md) · [ADR-037](../decisions/ADR-037-immersion-northwind-product-face.md)  
**Plan:** [blue-team-plan.md](../security/Cycle-8/Remediation/blue-team-plan.md)  
**Map:** [v2.5.0-remediation.md](../security/Cycle-8/Remediation/v2.5.0-remediation.md)  
**Residuals:** [accepted-residuals.md](../security/Cycle-8/Remediation/accepted-residuals.md)  
**Red evidence:** tag / branch `v1.5.0` / `ctf/v1.5.0` (frozen)

> Gate signed after Waves A–D + smoke (incl. `cycle6-blue-assert` + `cycle7-blue-assert` + `cycle8-blue-assert` + `assert-cycle8-unpublished`) + journey + e2e (166) + **tls-smoke** (2026-08-28).

---

## Product intent (delta vs `v1.5.0`)

| Domain | Secure `v2.5.0` |
|--------|-----------------|
| Intake | Kept — search behind `/api/intake/` without SQLi |
| SQLi / F1 plant | Closed — no dump; no F1 on tip |
| Overlays | Unpublished on default prod (`:21` / `:22` / internal SMB+mail) |
| Plants | No F1–F5 `OS{` on tip |
| Product face | **Northwind skin kept** (ADR-037); tip UI **v2.5.0** |
| Prior Blue | Preview SSRF + bookmark CSRF + Notes XSS + Ops path confinement remain closed |
| SSDLC | TLS profile required at this gate (inherit Cycle-6/7) |

---

## Deployment (verify on)

**Day-to-day HTTP (loopback residual C8-R01):**

```bash
docker compose -f infra/docker-compose.prod.yml up -d --build
./infra/assert-cycle8-unpublished.sh
./infra/assert-pg-unpublished.sh
./infra/smoke-test.sh && ./infra/journey-test.sh && ./infra/e2e-docker.sh
./infra/cycle6-blue-assert.sh && ./infra/cycle7-blue-assert.sh && ./infra/cycle8-blue-assert.sh
```

**Pre-tag TLS profile (required):**

```bash
./infra/scripts/gen-lab-certs.sh
docker compose -f infra/docker-compose.prod.yml -f infra/docker-compose.tls.yml up -d --build
./infra/tls-smoke.sh
```

**LAN / recruiter secure demo:** use the TLS profile. Do not advertise cleartext `:8080` on a reachable NIC as “secure.”  
**Insecure Cycle-8 replay:** pin tag/`ctf/v1.5.0` + `docker-compose.cycle8.yml` — not this tip.

---

## Findings — secure expectation

| ID | Secure expectation | Status |
|----|--------------------|--------|
| C8-F01 | Intake parameterized; no F1 / hash dump on tip | **Verified** |
| C8-F02 | No weak LIVE FTP on default prod tip | **Verified** |
| C8-F03 | No FTP→webroot RCE path on tip | **Verified** |
| C8-F04 | No sudo-nano privesc plant on tip | **Verified** |
| C8-F05 | No internal Samba/mail plant on tip | **Verified** |
| C8-F06 | Cowrie decoy archive-only | **Accepted residual** |
| C8-F07 | No decoy service-account cred panel | **Verified** |
| Prior | Preview / CSRF / Notes / Ops stay closed | **Verified** (smoke / cycle6–7 asserts) |
| SSDLC-TLS | tls-smoke green on TLS profile | **Verified** |

---

## Gate checklist

- [x] Wave A–C code complete on this branch
- [x] Intake blue negative assert green (`./infra/cycle8-blue-assert.sh` via smoke)
- [x] `./infra/assert-cycle8-unpublished.sh`
- [x] `./infra/assert-pg-unpublished.sh`
- [x] `./infra/smoke-test.sh`
- [x] `./infra/journey-test.sh`
- [x] `./infra/e2e-docker.sh` (166 tests)
- [x] `./infra/tls-smoke.sh` (TLS profile)
- [x] Tip UI / version shows `v2.5.0`
- [x] Bucket B FC-19 / FC-20 marked Consumed (Blue closed)
- [x] Sign-off below

**Signed:** Cycle-8 Blue (portfolio)  
**Date:** 2026-08-28
